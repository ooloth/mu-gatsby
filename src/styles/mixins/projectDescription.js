export const projectDescription = css`
  ${copy}
  margin-top: 0.6rem;
  max-width: 52ch;
`

///////////////////////////////////////////////////////////////////////////////////

import { css } from 'styled-components'

import { copy } from './copy'
